<% layout('../layout') %>
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Payment</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>


    <div>



        <table class="table table-responsive">
            <thead>
            <tr>
                <th>Payment_id</th>
                <th>PricePayment</th>
                <th>bank</th>
                <th>Time</th>
                <th></th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            <% orders.forEach(function(orders) { %>
            <% payment.forEach(function(payment) { %>
            <tr>
                <td><%= payment.orderids %></td>
                <td><%= payment.pricepayment %></td>
                <td><%= payment.bank %></td>
                <td><%= payment.timepayment %></td>
                <td>
                    <button type="button" class="btn btn-success">Update</button>
                </td>

            </tr>
            <% }); %>
            <% }); %>

            </tbody>

        </table>
    </div>
</div>
<!-- /#page-wrapper -->